<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/back"
    android:padding="20dp"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/iconIv"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginTop="40dp"
        android:layout_centerHorizontal="true"
        app:srcCompat="@drawable/logo1" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/iconIv"
        android:text="Bienvenido a la biblioteca escolar"
        android:layout_centerHorizontal="true"
        android:textSize="17sp"
        android:textColor="@color/black"
        android:layout_marginTop="10dp"/>

    <Button
        android:id="@+id/btnAccederMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/BtnRegistrarMain"
        android:backgroundTint="@color/botonacc"
        app:cornerRadius="18dp"
        android:layout_marginBottom="10dp"
        android:textColor="@color/black"
        android:text="Acceder"/>

    <Button
        android:id="@+id/BtnRegistrarMain"
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        style="?attr/materialButtonOutlinedStyle"
        app:strokeColor="@color/botonacc"
        app:cornerRadius="18dp"
        android:layout_height="wrap_content"
        android:text="Registrarse"
        android:textColor="@color/purple_500"/>



</RelativeLayout>